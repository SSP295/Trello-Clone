{
  "name": "trello-clone",
  "version": "1.0.0",
  "description": "A Kanban-style project management tool (Trello Clone)",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && python -m uvicorn main:app --reload --port 5000",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd server && pip install -r requirements.txt && cd ../client && npm install"
  },
  "keywords": ["trello", "kanban", "project-management"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
